/*
Just a simple sorting usage. Create a function that
returns the elements of the input-array in a sorted manner.
*/
sortme = function( names )
    {
    var names = ();
    names = int(input());
    console.log(names);
    names.sort();
    }
sortme();